# NEXT_STEPS.md

## 当前阶段判断

项目目标：

```text
4 周左右完成一个完整 Unity 小游戏；本阶段不实际开发微信小程序、macOS、Android 移植版本，但继续保持后续移植边界。
```

当前进度判断：

- 技术底座：约 75%。
- 核心玩法闭环：约 65%。
- 完整游戏体验：约 45%-50%。

已经完成：

- 建筑摧毁、废墟、重建基础。
- 大本营废墟聚兵。
- 敌方大本营被击败后的敌方清场。
- 士兵生成、移动、巡逻、聚兵、派兵。
- Neutral plot 占领主路径。
- 占领需求 Small/Medium/Large。
- U/O 结构化派兵结果。
- Q 扩张候选预览和可派兵统计。
- 占领成功 / blocked 后士兵停留目标附近巡逻。
- PlayerVictory / PlayerDefeat 最小胜负状态。
- 临时 HUD 显示目标、候选数量、最近结果、胜负状态。

## 四周冲刺路线

### 全程架构边界

- 核心玩法和平台能力分离。
- 输入 / UI 只发命令和读状态，不直接改核心数据。
- 后续触摸输入、移动端 HUD、平台存档可以替换当前键盘 / 临时 HUD。
- 不把平台判断散落到建筑、战斗、移动、地图逻辑中。
- 不为赶进度提交 Unity 生成目录或非必要 `ProjectSettings`。

### Week 1：核心可玩闭环

状态：基本完成。

- 占领成功后士兵围绕新占领 plot 巡逻。
- 占领失败后士兵停留/集结，不折返旧点。
- 最小 Victory / Defeat 状态。
- 临时 HUD 显示目标、候选、最近结果、胜负状态。

### Week 2：玩家正式操作与敌方压力

当前进入第一步：玩家操作。

- HUD 候选按钮选择扩张目标。
- gameplay command 服务承接 UI / 快捷键调用。
- 后续再做点击地图或正式 UI。
- 之后进入敌方最小 AI。

### Week 3：游戏性系统最小版

- 简化资源或人口上限。
- 建筑差异最小化落地：兵营 / 塔 / 粮仓至少有一个明确作用。
- 关卡节奏与数值初调。
- 简单提示和错误反馈。

### Week 4：打磨与交付

- UI 文案与状态反馈整理。
- 音效/特效/颜色反馈最小打磨。
- Play Mode 回归清单。
- 清理测试快捷键或隐藏到 debug 模式。
- 打包前检查：不提交 `Library/`、`Logs/`、`UserSettings/`、非必要 `ProjectSettings`。

## 当前正式任务：MVP-04.0 玩家正式操作第一步

目标：

```text
用临时 HUD 候选按钮选择扩张目标，并把 UI/快捷键统一接到 gameplay command 服务。
```

实现方向：

```diff
+ 新增 StrategicExpansionCommandService 或等价 command 服务
+ HUD 显示 expansion previews 列表和 Dispatch 按钮
+ O 快捷键复用同一条 command 服务路径
+ Q 继续只读预览
- 不做正式 UI 美术
- 不做世界点击 raycast
- 不做触摸输入实现
- 不做敌方 AI
- 不修改 ProjectSettings
```

## MVP-04.0 后的建议顺序

### MVP-04.1 敌方最小 AI

- 敌方定时派兵或攻击。
- 敌方目标选择简单明确。
- 玩家需要在压力下扩张和防守。

### MVP-04.2 正式输入整理

- 如果 HUD 按钮稳定，再决定做地图点击 / 目标选择高亮。
- 将 debug 快捷键集中到 debug-only 区域。

### MVP-04.3 完整游戏体验

- 简单资源/人口。
- 建筑功能差异。
- 胜负界面。
- 数值和反馈打磨。

## 长期提醒

- 现在不做移植实现，但后续仍可能做微信小程序、macOS、Android，因此业务逻辑边界必须持续清楚。
- 平台能力不要散落在建筑、战斗、移动脚本中。
- 当前最重要的是“可玩闭环优先”，但不能用平台耦合换速度。
