# NEXT_STEPS.md

## 下一步要求：MVP-01 固定地图与道路移动原型

依据 `goal.md`，下一步不进入完整战斗、AI、建筑和占领系统，而是先完成第一阶段最小可玩基础：

```text
固定地图
地块
道路
单位沿道路移动
```

本轮目标是让 Claude 建立 Unity 玩法骨架，验证这个游戏最核心的“路线争夺 / 区域控制”方向是否成立。

## 给 Claude 的任务

Claude 作为主要开发者，本轮可以开始做最小业务原型，但必须严格限制范围。

### 1. 建立清晰的脚本目录

在 `kingbattle/Assets/Scripts/` 下按最小需要创建目录：

```text
Scripts/
├── Core/
├── Map/
└── Units/
```

暂时不要创建完整大而全的系统目录。只有当本轮确实需要时才新增文件。

### 2. 固定地图原型

实现一个固定地图，不做随机地图。

地图至少包含：

- 5 到 7 个地块
- 地块大小：Small / Medium / Large
- 地块归属：Player / Enemy / Neutral
- 至少 1 个玩家主基地地块
- 至少 1 个敌方主基地地块
- 每个地块预留一个建筑槽位标记即可，不实现建筑系统

地块大小只需要先影响配置数据，不需要马上接入占领、建筑收益或血量。

### 3. 道路系统原型

道路是本游戏核心，本轮必须先验证。

要求：

- 地块之间通过固定道路连接
- 单位只能沿道路节点移动
- 不允许单位直接穿越地图空白区域
- 道路数据应和地块数据有清晰关系，避免写死在某个巨大 `GameManager` 里

可以先使用简单路点移动，不要求接入 A* Pathfinding 或 NavMesh。后续如要引入第三方寻路库，必须先说明理由。

### 4. 单位移动原型

实现 3 个兵种的基础移动配置：

- Samurai：速度最慢
- Elf Archer：速度中等
- Soldier：速度最快

本轮只验证移动速度和沿道路行走，不实现攻击、血量结算、占领消耗和出兵经济。

至少提供一个测试入口：

- 可以从玩家主基地生成一个测试单位
- 可以指定一个目标地块
- 单位沿道路移动到目标地块

测试入口可以是临时键盘触发或 Unity Inspector 配置，但必须保持代码清晰，并在文档里说明如何测试。

## 本轮禁止事项

Claude 本轮不要做：

- 不做战斗系统
- 不做 AI
- 不做占领进度
- 不做建筑建造 / 重建 / 拆除
- 不做粮食资源
- 不做英雄
- 不做技能树、装备、科技升级
- 不做联机
- 不做随机地图
- 不做复杂 UI
- 不修改 `ProjectSettings`，除非先说明理由并等待确认
- 不提交 `Library/`、`Logs/`、`UserSettings/`、`Temp/` 等 Unity 生成目录

## 架构要求

本轮代码必须体现以下边界：

- `Core/`：阵营、通用枚举、基础配置类型
- `Map/`：地块、道路、地图初始化或地图数据
- `Units/`：单位定义、单位移动、测试生成

禁止把所有逻辑堆进单个 `GameManager`。

允许有一个很薄的场景启动脚本，但它只能负责串联对象，不应承载地块、道路、单位移动的全部业务逻辑。

## 多平台边界要求

当前仍以 Unity 工程为主，不做微信小程序、macOS、Android 的具体适配。

Claude 需要避免：

- 在玩法逻辑中写平台判断
- 把输入、存储、支付、分享等平台能力散落在业务代码里
- 为未来平台提前创建空壳工程

如果本轮需要输入控制，只使用最小临时测试方式；正式输入系统等后续 UI / 操作阶段再设计。

## 验收标准

Claude 完成本轮后，必须满足：

- Unity 工程中出现清晰的 `Scripts/Core`、`Scripts/Map`、`Scripts/Units` 边界
- 场景中有固定地块和道路结构
- 至少一个单位可以从玩家主基地沿道路移动到目标地块
- 三个兵种的移动速度配置不同
- 单位不能绕过道路直接抵达目标
- Play Mode 无明显 Console Error
- 修改范围不包含 Unity 生成目录
- 已更新工作文档并完成 commit / push

## Claude 输出要求

Claude 提交后必须说明：

- 新增或修改了哪些文件
- 如何在 Unity 中运行测试
- 哪些内容是临时调试入口
- 哪些 `goal.md` 内容尚未实现
- 是否修改了场景文件
- 是否修改了 `ProjectSettings`
- 是否完成 commit / push

## Codex Review 重点

Claude 输出后，Codex 需要重点 Review：

- 是否符合 `goal.md` 的第一阶段优先级
- 是否过早实现了战斗、AI、建筑、占领或资源系统
- 是否出现超大脚本或隐形全局状态
- 道路和地块关系是否清晰
- 单位移动是否被限制在道路上
- 是否保留未来多平台边界
- 是否没有提交 Unity 生成目录

## 当前状态

```diff
+ `goal.md` 已明确 MVP 范围和长期目标
+ MVP-01 脚本实现已完成并提交审查
+ 12 个新增脚本文件覆盖地图、道路、单位移动
+ 固定地图包含 6 个地块、7 条双向道路
+ 三种单位速度差异已配置（Samurai 1.5 / Elf Archer 2.5 / Soldier 3.5）
+ 测试入口为键盘 Key 1-4
- 场景文件和 ProjectSettings 未修改
- 暂未执行 git add / commit / push（bash 环境不可用）
- 暂不批准战斗、AI、占领、建筑、资源、英雄系统开发
```

## 下一步（Codex Review 完成后）

Codex 审查结论：MVP-01 暂不批准进入 MVP-02。

Claude 下一步先修复 MVP-01 Review 项：

```diff
- 移除 MapData.Instance 全局状态入口
- 完成 Unity Play Mode / Console Error 验证
- 更新 WORKLOG.md 记录验证结果
- 再执行 commit / push
```

修复完成并通过 Codex 复审后，MVP-02 才预计包含：

- 建筑系统（箭塔 / 兵营 / 粮仓）
- 出兵系统
- 基础战斗

当前不要开始 MVP-02。
